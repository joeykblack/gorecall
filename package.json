{
  "name": "gorecall",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
  "build": "vite build",
  "postbuild": "cp sw.js docs/ && mkdir -p docs/icons && cp -r icons/* docs/icons/ || true && cp manifest.webmanifest docs/manifest.webmanifest 2>/dev/null || true",
  "preview": "vite preview"
  },
  "dependencies": {
    "@sabaki/sgf": "^3.4.7",
    "@sabaki/shudan": "^1.7.1",
    "preact": "^10.13.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "vite": "^5.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
